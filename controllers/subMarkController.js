
const subMarkController = {
    //add
    add: async(req,res) => {
        try {
            
        } catch (error) {
            res.status(500).json(`Error: ${error.message}`)
        }
    }
}

module.exports = subMarkController